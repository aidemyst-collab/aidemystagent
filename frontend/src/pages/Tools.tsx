import { Typography } from 'antd';

const { Title } = Typography;

export const Tools = () => {
  return (
    <div>
      <Title level={2}>Tools</Title>
      <p className="text-gray-600">Tool library will be displayed here.</p>
    </div>
  );
};
